How to run

Backend:
- You have to to create .env file in bin\Debug\net8.0 directory with the following keys:
    - DefaultConnection - database connection string
    - ASPNETCORE_ENVIRONMENT e.g. 'Development'

Frontend:
- Make sure you pass the backend URL as the first argument to the command line.
